# Important information about the Database

The database follows schema agnostic design. All table must follow id + json data structure so we can change data model without losing the ability for older database support. 